# .env.example

# OpenAI API anahtarınızı buraya koyun:
OPENAI_API_KEY=sk-...

# MCP sunucu URL’si (geliştirme için)
MCP_SERVER_URL=http://localhost:8000

# (Opsiyonel) Veritabanı yolu
DB_PATH=data/docs.db

# MCP auth’u atlamak için (yalnızca dev): true veya false
DANGEROUSLY_OMIT_AUTH=true
